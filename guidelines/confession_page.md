# 🔨 진정성 100% 반성문 생성기 — 기획 문서 v3

> **"당신은 이미 유죄입니다."**  
> 어떤 답을 해도 죄목이 확정되는 만우절 인터랙티브 심문 웹앱.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | 암쏘쏘리 |
| 컨셉 | 억지 심문 → 죄명 확정 → 반성문 작성 -> 반성문 공유 |
| 타깃 | SNS 공유용 바이럴 콘텐츠, 만우절 이벤트 |
| 핵심 UX | 어떤 답을 해도 죄 확정 / 아니요 연속 시 화면 흔들림 |

---

## 2. 전체 플로우

```
[시작 화면]
    ↓
[Q1 — 고정 첫 질문: "오늘 누군가에게 잘못한 일이 있으신가요?"]
    ↓ 예 → [입력창] → 죄 확정 → 반성문 생성
    ↓ 아니요
[Q_random — 나머지 27개 질문을 랜덤 셔플 후 순차 제공]
    ↓ 예 → (질문 유형에 따라) → 죄 확정 → 반성문 생성
    ↓ 아니요 → noCount++ → 다음 랜덤 질문
         ↓
    [noCount === 3]  살짝 흔들림 + 압박 메시지
    [noCount === 5]  중간 흔들림
    [noCount === 7]  강한 흔들림
    [noCount === 9]  심각한 흔들림
    [noCount === 10] 💥 강제 죄 확정: "현실 부정 및 반성 회피의 죄" → 반성문 생성
```

---

## 3. 질문 시스템

### 3-1. 첫 질문 고정
- `q1`은 항상 첫 번째로 표시
- 나머지 `q2~q28` (27개)는 매 세션 시작 시 **랜덤 셔플** 후 큐에 적재
- 이미 출제된 질문은 재출제 안 됨 (중복 방지)

### 3-2. 질문 답변 유형 (type)

| type | 설명 | 동작 |
|---|---|---|
| `confirm` | 예/아니요 단순 선택 | 예 → 죄 확정 / 아니요 → 다음 질문 |
| `input` | 예 선택 후 텍스트 입력 | 입력값이 죄명 설명에 템플릿으로 삽입됨 |
| `branch` | 예 선택 후 3지선다 | 선택지마다 다른 죄명 |
| `양방향` | 예/아니요 모두 죄로 귀결 | q5, q17, q18, q20, q22 등 |

### 3-3. 아니요 연속 카운터 (noCount)

```
noCount  shakeClass  화면 효과              메시지
──────────────────────────────────────────────────
3        shake-sm    0.3s, 2px 진폭        "...그래요?"
5        shake-md    0.4s, 5px 진폭        "정말요? 🤨"
7        shake-lg    0.5s, 8px 진폭        "저희 다 알고 있습니다."
9        shake-xl    0.6s, 12px 진폭       "화면이 흔들립니다..."
10       shake-max   폭발 애니메이션        → 강제 죄 확정 트리거
```

> **10회 아니요 강제 확정 죄명:**  
> *"현실 부정 및 반성 회피의 죄"*  
> 추가 혐의 10개 자동 부과 (아니요 1회당 죄 1개 누적)

---

## 4. 죄명 확정 화면

죄 확정 후 다음 데이터를 화면에 표시:

```
emoji   제목 (crime.title)
        설명 (crime.description / descriptionTemplate)
        중형 (crime.severity)
        [테마별 도장 이미지]
        [반성문 생성 버튼]
```

### 테마별 반성문 스타일

| theme | 스타일 | 대상 |
|---|---|---|
| `manuscript` | 원고지 | 일반 반성문 |
| `love` | 연애편지 | 연인 관련 죄 |
| `contract` | 각서 | 사회적·환경적 죄 |
| `resignation` | 사직서 | 직장 관련 죄 |
| `family` | 가족 반성문 | 가족 관련 죄 |
| `friend` | 친구 반성문 | 친구 관련 죄 |
| `health` | 건강 각서 | 건강·식습관 죄 |
| `digital` | 디지털 범죄 각서 | SNS·폰 관련 죄 |
| `environment` | 환경 범죄 각서 | 환경 관련 죄 |

---

### 5-5. 반성문 스타일별 출력 형식

#### manuscript (원고지)
```
반 성 문

저는 이번에 [죄목]을 저지른 [이름]입니다.
...
```

#### love (연애편지)
```
사랑하는 [상대방]에게

나는 당신에게 씻을 수 없는 죄를 저질렀습니다...
```

#### resignation (사직서)
```
사 직 서

소속: [부서명]
직위: [직위]
사유: [죄목]으로 인한 자진 사퇴...
```

#### contract (각서)
```
각 서

본인 [이름]은(는) [죄목]을 저질렀음을 인정하며...
```

---

## 10. 질문 카테고리 현황

| 카테고리 | 질문 수 | 질문 ID |
|---|---|---|
| 기본 (고정) | 1 | q1 |
| 연애 | 3 | q2, q3, q4 |
| 가족 | 2 | q5, q6 |
| 친구 | 3 | q7, q8, q9 |
| 직장 | 3 | q10, q11, q12 |
| 일상·디지털 | 4 | q13, q14, q15, q16 |
| 식습관·건강 | 3 | q17, q18, q19 |
| 환경 | 3 | q20, q21, q22 |
| 도덕·심리 | 6 | q23, q24, q25, q26, q27, q28 |
| **합계** | **28** | q1~q28 |

---

*본 문서는 만우절 프로젝트용 기획서입니다. 모든 죄목은 유머 목적으로 작성되었습니다. 🔨*
